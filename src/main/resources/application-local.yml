server:
  servlet:
    session:
      cookie:
        secure: ${SESSION_COOKIE_SECURE:false}

app:
  receipt:
    storage:
      type: ${RECEIPT_STORAGE_TYPE:local}
    scanner:
      type: ${RECEIPT_SCANNER_TYPE:eicar-test}
